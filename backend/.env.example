# ========================================
# Flask Backend 环境变量配置模板
# ========================================
#
# 使用说明:
# 1. 复制此文件为 .env
#    cp .env.example .env
#
# 2. 修改 .env 中的值为你的真实配置
#
# 3. ⚠️ 重要：.env 文件包含敏感信息，已添加到 .gitignore，不会推送到 GitHub
#

# ========================================
# 数据库配置
# ========================================

# SQLite 数据库文件路径
# 开发环境：使用相对路径
# 生产环境：使用绝对路径
DATABASE_PATH=gym_roi.db

# 或使用绝对路径（推荐）
# DATABASE_PATH=/Users/你的用户名/Documents/duckiki/backend/gym_roi.db

# ========================================
# Flask 应用配置
# ========================================

# Flask 密钥（用于 session 加密）
# ⚠️ 生产环境请使用长随机字符串！
# 生成随机密钥: python3 -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change-this-to-a-random-string

# Flask 运行模式
# development: 开发模式（自动重载、详细错误）
# production: 生产模式（性能优化、隐藏错误详情）
FLASK_ENV=development

# 是否开启调试模式
# True: 代码改动自动重启，显示详细错误堆栈
# False: 不自动重启，隐藏错误详情
FLASK_DEBUG=True

# Flask 运行端口（默认 5000）
FLASK_RUN_PORT=5000

# Flask 监听地址
# 127.0.0.1: 仅本地访问
# 0.0.0.0: 允许局域网访问（仅开发环境使用）
FLASK_RUN_HOST=127.0.0.1

# ========================================
# CORS 跨域配置
# ========================================

# 允许的前端地址（多个地址用逗号分隔）
# 开发环境：Vite 默认端口 5173
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 生产环境示例：
# CORS_ORIGINS=https://chenmq77.github.io

# ========================================
# 数据导出配置
# ========================================

# JSON 文件导出路径（相对于项目根目录）
EXPORT_DATA_PATH=src/apps/gym-roi/data

# 是否在导出时脱敏数据
# True: 移除敏感个人信息（推荐）
# False: 导出完整数据
DATA_SANITIZATION_ENABLED=True

# ========================================
# ROI 计算配置
# ========================================

# 默认货币
DEFAULT_CURRENCY=NZD

# 汇率（NZD to RMB）
EXCHANGE_RATE_NZD_TO_RMB=4.1

# 游泳权重计算参数
SWIMMING_BASELINE_DISTANCE=1000  # 基准距离（米）
SWIMMING_SIGMA=400               # 标准差

# ========================================
# 日志配置（可选）
# ========================================

# 日志级别
# DEBUG: 详细日志
# INFO: 一般信息
# WARNING: 警告信息
# ERROR: 错误信息
LOG_LEVEL=DEBUG

# 日志文件路径（留空则不写入文件）
LOG_FILE=

# ========================================
# 其他配置
# ========================================

# 时区设置
TIMEZONE=Pacific/Auckland

# 每页返回数据数量（分页）
PAGE_SIZE=20
